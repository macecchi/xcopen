#!/bin/bash
EXT='xcworkspace'
dir=$1

shopt -s nullglob

found=0
for i in $1/*.${EXT}; do
    open -a Xcode "$i"
    found=1
    break
done
[ $found -eq 0 ] && echo "No Xcode workspace found"

shopt -u nullglob

